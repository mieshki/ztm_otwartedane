<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ztm_otwartedane"
             x:Class="ztm_otwartedane.MainPage">

    <StackLayout>
        <SearchBar
            Placeholder="Wpisz nazwę przystanku..."
            TextChanged="FilterBusStops_TextChanged"/>
        <Button
            Text="Update bus stops"
            Clicked="btnUpdateBusStop_Clicked"/>
        <Label
            x:Name="testLabel"/>
        <ListView x:Name="listViewBusStops">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Label
                            Text="{Binding name}"/>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
